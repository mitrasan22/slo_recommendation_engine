apiVersion: v1
kind: Service
metadata:
  name: slo-api-service
  namespace: slo-engine
  labels:
    app: slo-api
spec:
  selector:
    app: slo-api
  ports:
    - name: http
      port: 80
      targetPort: 8000
      protocol: TCP
  type: ClusterIP
